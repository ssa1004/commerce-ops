# CODEOWNERS — PR 이 특정 경로를 건드리면 자동으로 review 요청이 가는 owner 매핑.
# 형식: <path-glob>  <@owner...>. 마지막에 매칭된 규칙이 우선한다.
#
# 단일 maintainer 레포라 전역 owner 는 한 명이지만, 경로별로 명시해 두면
# (1) review 누락 방지 (2) 나중에 팀이 늘 때 소유 경계가 이미 그려져 있다.

# 기본 — 명시되지 않은 모든 파일.
*                               @ssa1004

# 서비스 코드 (order / payment / inventory).
/services/                      @ssa1004

# 공용 운영 라이브러리 모듈 — 7개 sister repo 가 dependency 로 가져다 쓰므로
# 변경 영향 범위가 넓다. 항상 owner review.
/modules/                       @ssa1004

# 배포 surface — Helm chart / values. prod 동작에 직결.
/helm/                          @ssa1004

# 옵저버빌리티 / 인프라 설정 (Prometheus rule, Alertmanager, OTel, compose).
/infra/                         @ssa1004

# CI / 공급망 / 의존성 자동화.
/.github/                       @ssa1004
