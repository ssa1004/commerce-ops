{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "payment-service.secretName" . }}
  labels:
    {{- include "payment-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_PASSWORD: {{ .Values.global.postgres.password | quote }}
{{- end }}
